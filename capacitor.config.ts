import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.citizenconnect.pakistan',
  appName: 'Citizen Connect Pakistan',
  webDir: 'dist'
};

export default config;
